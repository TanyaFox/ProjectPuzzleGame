<Window x:Class="PuzzleGame.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Паззлы" Height="300" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" Margin="20">Добро пожаловать, дорогой игрок!</TextBlock>
        <Button Grid.Row="1" Height="30" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 5 0 5" Command="{Binding ButtonNewGameCommand}">НОВАЯ ИГРА</Button>
        <Button Grid.Row="2" Height="30" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 5 0 5" Command="{Binding ButtonSavedGameCommand}">ПРОДОЛЖИТЬ ИГРУ</Button>
        <Button Grid.Row="3" Height="30" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 5 0 5" Command="{Binding ButtonAuthorsCommand}">АВТОРЫ</Button>
        <!--<Button Grid.Row="4" Height="30" Width="150" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 5 0 5" Command="{Binding ButtonExitCommand}">ВЫХОД</Button>-->
    </Grid>
</Window>
